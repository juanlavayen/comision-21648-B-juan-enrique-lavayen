<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Great Post</title> <!-- Título de la página -->
    <!-- Enlaces a las hojas de estilo -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <!-- Encabezado -->
    <header>
        <!-- Barra de navegación de Bootstrap -->
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">Great Post !!</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarText">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="#">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Posts</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/posts/create">Nuevo Post</a>
                        </li>
                    </ul>
                    <span class="navbar-text">
                        Face Inst Etc...
                    </span>
                </div>
            </div>
        </nav>
    </header>
    
    <!-- Contenido principal -->
    <main>
        <h1>Sistema de Post Great-Post:</h1>
        <!-- Tabla para listar los posts -->
        <div class="tabla">
            <!-- Cabecera de la tabla -->
            <div class="cabecera celda">Titulo</div>
            <div class="cabecera celda">Contenido</div>
            <div class="cabecera celda">Link</div>
            <div class="cabecera celda">Fecha</div>
            <div class="cabecera celda">Accion</div>

            <!-- Filas de la tabla -->
            <% allPosts.forEach((post) => { %>
                <div class="celda"><%= post.titulo_post %></div>
                <div class="celda"><%= post.contenido_post %></div>
                <div class="celda"><a href="<%= post.link_post %>"><%= post.link_post %></a></div>
                <div class="celda"><a href="<%= post.link_post %>"><%= post.link_post %></a></div>
                <div class="celda"><a href="/posts/update/<%= post.id %>">📝</a><a href="/posts/delete/<%= post.id %>">❌</a></div>
            <% }); %>
        </div>
    </main>
    
    <!-- Pie de página -->
    <footer>
        <div><p>Pagina web Great-Post @copyrigth Juan Lavayen 2023 EPICA</p></div>
    </footer>
    
    <!-- Scripts de Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
</body>
</html>
